# 声伴项目协作规则

以下版本接续流程由用户于 2026-09-12 明确要求，适用于本项目后续开发任务。

- 先读 `docs/功能总表.md`、`docs/实施与验收计划.md`、`docs/版本接续.md` 和 Git 状态。已实现、已通过的内容不要因旧方案文字再开发或要求用户重复整套验收；只处理本版本增量和有证据的回归。
- 每个开发任务标题包含目标版本，例如“声伴 v0.6.17 · 短时连续接话”。根目录 VERSION 表示代码实际完成的版本，不能只因创建新任务提前宣称完成。
- 新建或接续任务后，不能只凭创建回执中的标题宣称可被声伴查到；用正式任务命名工具确认名称持久化，再只读核对声伴实际候选列表中同一任务 ID 的名称。名称为空时明确报告或恢复已确定的名称，不从聊天正文猜标题、不修改 Codex 私有数据库。
- 每版围绕一个明确、可验收的范围。完成实现、相关测试、生成物清理、文档与 Git 提交后再收口；用户已要求将本轮源码上传现有 origin。后续沿用同一版本交付流程，遇到认证、分支冲突或明确拒绝时如实说明，不能强推或绕过。
- 收口后，用 Codex 任务工具创建下一版本任务并传递边界、提交、验证结果和剩余需求；确认新任务已创建且开始接手后，归档刚完成的开发任务。用户已授权按版本自动接续，无需每版重复征求创建与归档同意。只归档本项目已收口的对应任务，不能归档其它工作。
- 接续只覆盖已记录需求池；需求完成、被用户叫停或下一项存在必须由用户决定的阻碍时停止。不要创建空任务、为凑版本改文案，或无限增加未经要求的功能。归档是开发工作收口，不代表真人声学、异机安装或公开发行已验收。
- Git 仓库任务按工具要求使用工作树，先核对实际分支与基线；交付前安全同步，不覆盖用户改动，不强推、不重写已公开历史。工作树缺少 runtime 是源码设计，不复制个人 data 或整个开发环境来伪装可复现安装。
- 测试与验证临时产物用完清理，只保留可复用回归源码和简短结果；不要删除用户模型、个人设置或其它进程持有的文件。已有被审批明确阻止的操作不得重试或绕过。
- 含中文的 PowerShell 脚本保持 Windows PowerShell 5.1 兼容和 UTF-8 BOM。保护草稿、明确投递目标、结果未知不重派、旧异步结果失效等现有约束持续有效。
- 不用真实聊天、任务管理或背景录音做自动化测试。新的持续接话能力默认关闭，实际用户启用及真人验收与离线回归分开记录。微信入口维持暂不开发。
