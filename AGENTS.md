# 声伴项目协作规则

以下规则以用户于 2026-09-22 明确的版本流程为最新准则；与 2026-09-12 旧接续文字冲突时，以本文件和 `docs/项目流程与版本规范.md` 为准。

- 开始任何项目任务前，先读 `docs/README.md`、`docs/功能总表.md`、`docs/问题池.md`、`docs/实施与验收计划.md`、`docs/版本接续.md` 和 Git 状态。已实现、已通过的内容不要因旧方案文字再开发或要求用户重复整套验收；只处理当前获准的小项和有证据的回归。
- 项目固定分为总控、程序、审核三个角色，任务标题必须分别以 `总控｜`、`程序｜`、`审核｜` 开头，并包含版本与范围。总控只做架构、范围、风险裁决和调度；程序负责实现、相关测试和候审提交；审核使用每个重大迭代新建的独立任务，只读审查并向总控报告，不改代码、不提交、不部署。
- 新建或接续任务后，不能只凭创建回执中的标题宣称可被声伴查到。用正式任务命名工具确认名称持久化，再只读核对声伴实际候选列表中同一任务 ID 的名称。名称为空时明确报告或恢复已确定的名称，不从聊天正文猜标题、不修改 Codex 私有数据库。
- 每次只交付一个明确、可验收的小项。用户说“可以了”只接受当时明确的小项；指代不明时先澄清。当前小项通过前，不开始同版本下一项；审核通过也不自动等于用户验收。
- 标准链路为：需求与根因证据 → 先写会失败的回归 → 最小修复 → 新鲜实际输出 → 独立审核 → 总控裁决 → 用户单项验收。断言数量只说明运行规模，不能替代覆盖范围、失败先行证据或真人/设备/异机验收。
- 只有用户明确说“收口”才触发版本总结、工程门槛、创建下一版本总控任务、确认接手和归档。下一版本总控先汇总问题池并让用户确认计划，确认后才委派程序，不自动开始实现。
- 收口归档只覆盖已确认属于当前版本且已完成的总控、程序、审核任务；不删除聊天，不归档其它项目、未完成任务或归属不明的任务。任务归档不代表真人声学、异机安装或公开发行已经验收。
- 根目录 `VERSION` 表示代码实际完成的版本，不能因计划或任务标题提前修改。0.x 阶段按 SemVer 使用：修复递增补丁号；功能或较大迭代递增次版本并将补丁归零；1.0 只用于稳定公开契约。alpha/rc 仅在构建兼容性核验后使用；正式发布内容不可用同一版本号替换，后续修复必须新开补丁版本。
- 候选交付必须写完整提交 SHA。Git 仓库任务按工具要求使用工作树，先核对实际分支与基线；上传、部署、创建下一版和归档都必须符合当时总控裁决与用户授权。不得强推、重写已公开历史或覆盖用户改动。
- `docs/问题池.md` 是去重索引，分类只能为 `重要`、`普通`、`待定`。新需求先记录来源、现象、证据、理由、状态、目标版本和验收证据；进入问题池不等于进入当前版本。
- 保留 `src/`、`tests/`、`tools/`、`runtime/`、`data/` 的职责边界。目录迁移必须先有只读盘点、映射、回退和总控/用户批准；不批量移动源码、模型、个人数据或被策略拒绝处理的文件。源码工作树缺少 `runtime/`、`data/` 是设计边界，不复制个人环境来伪装可复现安装。
- 测试与验证临时产物原则上精确清理；已有策略明确拒绝的清理不得重试、换命令绕过或搬运，须记录精确路径、数量、字节数和 Git 边界。不要删除用户模型、个人设置或其它进程持有的文件。
- 含中文的 PowerShell 脚本保持 Windows PowerShell 5.1 兼容和 UTF-8 BOM。保护草稿、明确投递目标、结果未知不重派、旧异步结果失效等现有约束持续有效。
- 不用真实聊天、任务管理或背景录音做自动化测试。新的持续接话与免唤醒能力默认关闭，实际用户启用、真人验收与离线回归分开记录。微信入口维持暂不开发。
- 0.7.0 目前只允许规划“可配置模型接入，用户醒来后自行填写服务”。参考用户本地整合方案，但不得在未批准前实现、上传对话或音频、产生付费、自动打开全天免唤醒，或加入微信/飞书入口。
