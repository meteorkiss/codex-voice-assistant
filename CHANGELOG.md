# 更新记录

## 0.6.17 · 2026-09-12

- 2026-09-13 本地容错增量：用户确认先做本地关键词及候选确认、不加小模型。任务名按空格/中英边界/版本拆分，口令识别任务里/里面/里边/中等尾缀；明确字面唯一结果直接校验连接，英文有限拼写偏差、中文同音与已知别名均需确认。一个候选支持唤醒后“是的/不是”，多个候选需编号，普通新话题撤销待确认状态；数字不纠错、旧回执与草稿保护不变。无新增依赖或持续录音。
- 2026-09-13 截图口令回归：补齐“把任务/对话/聊天切到……”入口，有限去除名称前的“这个/那个”；截图两句不再落入普通聊天。已知“申办→声伴”别名只在有明确版本号且字面匹配失败后尝试，保留其它关键词，多个匹配仍需选择。版本号不纠错；找不到会播报实际识别目标并保持原连接。
- 用户已现场确认设置选中即连接、随后语音进入所选任务、微信截图正常；上述“把任务切到……”语音回归单独验收，不要求重复前三项。
- 2026-09-13 设置交互增量：移除“连接所选任务”按钮，用户选中后自动校验并连接；列表刷新/筛选不代选，失败回显原实际目标，快速改选使旧连接回执失效。保留草稿先存后清及忙碌/未知发送保护。
- 设置面板独立为普通非置顶窗口，不再继承悬浮球 Owner 或原生置顶守护；悬浮声波与字幕继续遵循置顶偏好。微信截图已由用户现场确认正常，非自动化代测。
- 修复已绑定任务归档后仍显示健康、旧草稿阻止监听及重新连接：后台只读核验明确归档状态，停用普通发送但保留唤醒后的本地切换入口；归档、缺失和未知分别处理，不猜默认任务或自动恢复归档。
- 明确点击连接时先把旧草稿保存到本机 `data/recovery-drafts`，保存成功才清操作输入；失败保留原文。归档恢复中的普通语音同样只保存不发送，新绑定不自动带入旧文字；设置页提供查看入口及“选中但未连接”提示。
- 所有发送回执按绑定/输入代次及原文核对；发送前再次校验归档，未知旧请求仍不重派。独立状态检查不占语音请求通道，归档后的监听故障恢复也保留本地口令能力。
- 修复列表和语音搜索使用首句、忽略 Codex 侧栏改名：只读名称索引覆盖已有可信候选，不扩大目标范围；坏行/半行不覆盖，缺少名称时提示。
- 支持“切到6.17”“切刀声伴6.17”“切到高斯泼溅”和空格关键词组合；6.17 可对应 0.6.17，但不猜 6.170、16.17、6.17.1 或 617。多个匹配保留选择，不引入语音大模型。
- 设置页说明目标对话是语音投递位置，并给出关键词例句。离线结果及部署/草稿状态分开记录，不能由版本号推断补丁已加载。
- 修复归档任务记录移动后反复报错并中断录音：只恢复同一完整文件名及游标，无法恢复时保留草稿和本地切换、暂停普通消息。
- 本地切换支持“帮我切换到声伴 V0.6.17 任务”及命令位置的“切换刀”；版本点不再被误当断句，重名和版本不符不猜目标。
- 修正接话 6 秒仅限制首次开口、30 秒到限只留草稿，以及取消后旧转写/回执/回答和迟到唤醒消息的保护。
- 本次修正另通过解析 1290、匹配 24、真实 Tick 替身 330、记录恢复 12、C# 接话边界 3 项。新增切换集成样例未执行：临时复制/三组运行/递归清理的组合命令被审批拒绝，未重试或绕过。统一入口登记为 18 组；下文 17 组通过是修正前首版的结果，不能算本次全量通过。
- 增加默认关闭的短时连续接话：仅在明确唤醒问题成功发送、对应回答自然朗读结束后开放 6 秒窗口，单句最长 30 秒。
- 全双工就绪时把仍在运行的回声消除采集直接交给接话录音，避免界面交接截掉句首；其它设备路径等麦克风真正就绪后才显示正在听。
- 草稿编辑、任务或筛选变化、连接异常、外部麦克风占用、闲置超时与显式结束都会撤销接话代次；迟到转写或回答无权重新开启窗口。
- 短语气词等模糊转写只保留为草稿，不自动派发；设置页、字幕操作、声波菜单和托盘提供可见状态及结束入口。
- 离线入口扩展为 17 组并全部通过；新增连续接话状态机、设置恢复、C# 采集接口编译与空闲保护检查。未进行真人语音、背景媒体、异机安装或公开发行验收。

## 0.6.16 · 2026-09-10

- 提取 AudioOutput，统一合成启动、完成交接和停止清理；主程序保留薄入口。
- 合成启动失败清理请求和部分音频，播放失败释放本次结果；活动合成不能被另一次启动覆盖。
- 合成结果携带任务与代次，过期结果不播放；停止失败仍取消待播内容，保留正在使用的音频供再次释放。
- 续读书签路径拒绝越界、目录和重解析点，清理只删除归属当前运行的文件。
- 增加音频输出故障回归、源码依赖与私人/生成文件入库检查，以及 Windows 离线 CI 配置；CI 尚未在远端运行。

## 0.6.15 · 2026-09-10

- 提取 PreferenceActions，声音、语速、置顶、字幕、悬浮声波、自动朗读、自动发送、样式和尺寸共用校验、保存及失败回退；唤醒和音频模式继续由原模块管理。
- 语音显示字幕时，字幕与悬浮声波作为一次设置事务保存；保存失败同时恢复内存、控件和窗口状态。
- 共用已消费口令的清理逻辑，设置完成时保留新草稿和新发送意图；活动候选操作返回未处理时仍在本地报告失败，不进入普通聊天。
- 提取 CodexAdapter，派发前核对操作用途与编号；Python 继续负责真实能力、目标和防重派账本。
- 管理回执严格检查布尔类型、请求编号和操作名；缺失或错误类型按结果未知处理，重复成功回执不重复朗读。
- 新增对应故障与真实控件回归，沿用临时副本自动清理；补齐启动器模块依赖。

## 0.6.14 · 2026-09-10

- 从主程序提取 Settings、PendingSends、Persistence 与 WorkerLifecycle，保留设置和发送账本格式及原数据位置。
- 配置与发送记录使用同目录唯一临时文件和原子替换，失败保留旧文件并清理本次临时文件。
- 后台进程确认退出后再释放文件；取消不能终止已派发的发送、创建或管理操作。文件清理拒绝越界路径和重解析点。
- 派发前失败清理本次请求；派发后进程信息保存失败仍保留发送锁和后台任务，继续等待原回执。
- VERSION 作为程序与启动器的版本来源；构建核对 README 和 CHANGELOG，失败保留原 exe，清理生成的元数据和候选文件；启动器检查新模块及源码版本一致性。
- 扩充隔离回归，覆盖配置恢复、文件锁、派发故障、进程取消和构建失败；临时文件随测试结束清理。

## 0.6.13 · 2026-09-10

- 提取 TaskBinding，共用目标校验、绑定提交、设置保存失败回退和任务选择同步。
- 手动连接保护现有草稿、待发送内容和未知回执；连接期间的新输入、改选、录音、取消、超时与刷新使旧请求失效。输入后又删空、选择后又选回也不会恢复旧请求。
- 语音切换及创建后连接同步保护新的文本编辑；创建结果仍保存，不重复创建。消费已执行口令时的清空与新输入分开处理。
- 启动恢复采用同一连接入口，补齐启动器必要模块检查。
- 增加隔离回归入口，使用临时源码副本，成功或失败都清理生成的数据；保留可重复运行的测试源码。
- 建立本地 Git 基线与远端关联，未推送；增加源码忽略及编码约定。

## 0.6.12 · 2026-09-09

- 增加本地任务与分组管理、指定任务状态及回答读取，操作结果核对与持久防重派。
- 增加语音暂停、继续与重读，唤醒时保存播放位置及剩余内容。当前任务归档与项目移动等边界见口令说明。

## 0.6.11 · 2026-09-09

- 扩充“新对话”“新任务”“帮我弄一个新对话”等完整口语新建指令。

## 0.6.10 · 2026-09-09

- 补齐“创建新任务”“创建一个新对话”等说法，匹配后直接进入本地创建流程。

## 0.6.9 · 2026-09-09

- 增加采集和模型处理进度检查、有限恢复及默认设备路由变化处理；真人漏唤醒的效果仍需独立验证。

## 0.6.8 · 2026-09-09

- 麦克风会话每次扫描重新核验所属进程，忽略已确认退出的进程残留；权限不足、查询失败及未知所属仍保留录音保护。
- 其他应用使用麦克风时，明确显示语音唤醒暂缓；补充唤醒事件、音量与麦克风扫描的只读诊断状态。
- 保留 0.35 唤醒门槛、当前任务及声音设置。已完成相关回归并加载；原始漏检与重启后残留会话阻塞分开记录，见 [排查记录](docs/唤醒无回应排查-20260909.md)。

## 0.6.7 · 2026-09-08

- 增加单项语音新建口令，默认创建独立聊天；明确指定“当前项目”时，只使用与当前绑定任务目录唯一匹配的已保存本机项目。创建新任务不改变已有任务的项目归属。
- 新任务沿用 Codex 配置的默认模型与思考设置，不复制原任务的模型覆盖。创建成功后先读取校验，再保存新绑定；可选任务名称。
- 增加“连接刚才的新任务”和“放弃连接新任务”。创建请求与回执持久化，不确定结果不重复派发；未连接时保留问题草稿，不发往旧任务。迟到回执只记账，不能覆盖后来变化的绑定或草稿。
- 仅有临时客户端编号时保留待核对状态，需在 Codex 确认后手动连接；放弃连接不删除任务，也不终止已经派发的创建。
- 增强低音量下的声波形变，并调整外扩以保持边界，不调整真实播放音量；静音和暂停继续保持安静。
- 唤醒关键词默认门槛从 0.5 调整为 0.35，已有低音量与加噪文件样本表现改善；真人效果和监听准备空窗仍待验证，见 [唤醒漏检排查](docs/唤醒漏检排查.md)。
- 已重新加载本机 0.6.7，原任务和偏好保留，连接、回声采集与置顶就绪。新建流程只使用隔离数据和桥接替身回归，本轮未实际创建任务或发送测试消息。

## 0.6.6 · 2026-09-08

- 朗读省略单独列出、附在句尾或括号中的参考链接与脚注，保留正文中的语义链接文字；字幕与原答案不变。
- 增加“切换到某某任务”本地口令。按标题完整匹配、关键词包含、至少三个汉字的同音匹配依次搜索，不下载新模型。
- 唯一结果读取校验后保存绑定；多个结果展示原始标题并支持“选择第二个任务”“取消切换”。超时、录音、草稿或任务变化使旧结果失效。
- 切换失败保留原任务，不把已识别的切换口令发给 Codex；实际问题沿用新任务的模型与权限。手动刷新、筛选、绑定和普通发送清理旧候选。
- 修复回声处理重新启动时清掉尚未朗读的操作反馈：提示文字等待设备就绪，外部录音及明确取消仍按原规则中断。

## 0.6.5 · 2026-09-08

- 中央改为两个淡色圆形按钮：播放／暂停与结束朗读；结束会取消当前朗读、待播段落和语音合成，保留答案及 Codex 任务。
- 按用户反馈移除空格播放快捷键及对应模块。
- 播放器从实际解码音频计量 RMS 音量；修复唤醒监听覆盖播放音量的问题，去掉朗读的固定正弦节奏与重复平滑，静音和暂停不再模拟说话。
- 置顶使用原生窗口层级，显示时恢复，切换到其他程序时重申一次；保持当前输入焦点，取消置顶和隐藏仍正常生效。

## 0.6.4 · 2026-09-08

- 声波中央加入淡色半透明播放／暂停按钮，悬停时稍微增强显示；已暂停时继续原音频，未播放时可播放上一条回答。
- 按钮之外仍保持透明与原声波拖动区域，中心按钮右键沿用原设置菜单。
- 增加声伴窗口内空格暂停／继续，保留输入框及其它控件的正常键盘操作，不拦截其它软件的空格。

## 0.6.3 · 2026-09-08

- 唤醒词可在声音设置中编辑、保存、恢复默认；增加输入校验，保存时只重载唤醒监听。
- 增加右键、托盘和字幕中的暂停／继续，暂停保留播放位置和待播内容。
- 修复打开设置、读取任务列表时错误释放回声采集，从而中断朗读的问题。
- 本轮未修改免手录音的时长和自动发送规则。

## 0.6.2 · 2026-09-08

- 常用设置可直接用口头指令调整，包括声音、语速、置顶、字幕、悬浮显示、自动朗读与样式。
- 明确的单句指令在本机执行和保存，通过原有声音给出简短反馈；不用另装大语言模型，也不为设置操作向 Codex 发送消息。
- 普通提问、引述、否定和多项混合请求继续走原有 Codex 路径；保留录音、过期输入和未知发送回执的检查。

## 0.6.1 · 2026-09-08

- 根据实际试用反馈重做声波：连续流动曲线、柔和光泽与更平缓的音量响应，可见时独立刷新动画。
- 设置改为连接、声音、外观三页；使用中性色、细分隔线和少量蓝色强调，去掉宣传性文案与绿色装饰卡片。
- 字幕同步为简洁浅色样式，继续保留展开输入、录音和发送入口。
- 保留原来的样式编号、个人偏好、任务绑定及全部语音功能。

## 0.6.0 · 2026-09-08

- 桌面改为透明声波，加入六种样式、独立设置、托盘菜单与可展开字幕。
- 设置内直接筛选目录、下拉选择任务；初次运行不再使用私人默认任务。
- 绑定校验失败时保留一致的旧绑定；发送回执严格检查布尔类型并保留未知状态。
- 声音与语速独立选择；普通界面操作不打断朗读，开关和位置保存到本机。
- 朗读跳过预览元数据、代码、网址与路径，保留完整文字供查看。
- 新增 Windows 启动器、固定播放设备的音频组件及回声消除验证模块。

此版本在本机开发和验证，公开安装与其他设备兼容性另行验收。
