# 声伴 0.6.17 交付审核

最新交接状态（2026-09-21）：普通 push 已成功上传至 `436d349`，下文部署时的未上传记录为历史。用户现明确要求上传后新建 0.6.18 免唤醒任务，按[交接](免唤醒交接-0.6.18.md)先架构后实施；原受阻测试/清理事实保留，未宣称豁免或完成，本任务暂不归档。

日期：2026-09-21。初审基线：`e4bb448`，核心接话修复 `2867e37`，独立启动连接修复 `8c6f436`。VERSION 保持 `0.6.17`。本文最初是原任务自审；用户随后明确要求新对话独立审核、回报原任务修复，现按该流程更新。不代表安装包发行验收。

## 独立审核发现及修复复核

独立审核任务 `01a0c405-c5f2-70c0-b758-aaabd8146902` 发现 P2：完成回答可能先于发送回执到达，本地 tail 已消费回答而 waiting-answer 会话尚未建立；迟到 accepted 之后再也等不到该回答，下一轮接话无法开启。原测试固定先回执再回答，漏掉相反时序。独立报告推翻下文自审“未发现”的充分性；该段仅保留为历史结果。

原任务修复：早到回答仍立即显示，仅当前精确匹配的语音发送可在自己的 job 中暂存一条回答与请求/目标/代次快照，暂缓这条回答朗读。accepted 通过既有 currentReceipt 校验、创建等待状态并安全清稿后，复核缓存及当前用户轮次，再登记回答、排队朗读；拒绝、未知、取消和失效上下文不使用缓存。这样极短回答也不可能在确认前读完而遗漏接话；不暂停普通历史回读，不重发，不新增持久化或录音。

同时修复同一 Tick 中 worker 在回执检查之后、回答读取期间退出的竞态：当前精确 job 由下一 Tick 分类回执，不凭瞬间 HasExited 提前撤销会话；缺失/未知回执仍关闭接话并保留发送锁。成功回执不再拥有当前绑定/输入时，只关闭同代次旧接话，不抹新草稿或关闭新会话。

验证：新增早到回答断言在修复前失败；修复后 `Test-HandsFree.ps1` 837 断言通过，包含 wake/follow-up 两种来源、正常/立即播放完成、中途退出、拒绝/未知及目标/绑定/input/voice/token/草稿/取消/关闭/后来轮次的失效边界，旧两轮 pending 链路仍保留。归档恢复 88、语音切换 380、自动连接 215、源码声明 46 同轮复验通过。新生成物自行精确清理，原 27 个历史文件保持不变；未操作真实音频或任务。

独立复核已回报通过：对方只读核验稳定差异，独立运行免手 837、归档恢复 88、源码声明 46，检查 BOM 与 diff；确认原 P2 及相邻安全边界修复，未发现新增阻断缺陷。复核不代表完整无缺陷或豁免既有收口条件。部署与上传结果另见版本接续，未正式收口。

修复提交 `53e32d9` 已于 21:21:49 本地部署，原目标/偏好保留、监听健康；两次普通 push 连接重置，截至部署尚未上传。当前需要完成的是正常远端同步及旧测试/27 文件例外决定，不要求用户再做整套验收。此前文件中的旧部署/上传描述保持历史含义，以本节和版本接续顶部为准。

## 原任务初审结论（历史，以上方独立复核为准）

本轮重点审查及 7 组独立离线复验未发现新的阻断缺陷。用户现场确认本轮测试没有问题，可以结束这轮真人验收；手机视频被误收并发送仍是已知限制，用户明确暂时接受，不标为修复或防误收通过。

**暂未正式收口、接续或归档。** 既有收口规则要求相关测试与生成物清理完成；旧组合测试未执行，27 个历史生成文件清理曾被明确拒绝。本轮不重试、不改名换路径执行，也不删除这些文件。需要用户明确决定是否把它们作为收口例外保留；普通“处理完再收口”不自动等于豁免。

## 审查范围与代码证据

| 范围 | 核查结果 |
| --- | --- |
| 接话状态与回执 | `HandsFree.ps1` 只对当前 dispatching、进程仍存活且请求/目标/绑定、输入、语音及接话代次/原文全部匹配的发送等待原回执；保留超时、取消、新输入与未知结果保护。 |
| 成功清稿 | `Assistant.ps1` 接受回执仍核对真实目标、代次和原文；先切到等待回答状态再清旧输入，避免 TextChanged 误取消；旧回执不能抹掉新草稿。 |
| 发送账本 | `PendingSends.ps1` 先持久化再同步状态，保存失败回滚；明确接受/拒绝才结算，未知不重派。 |
| 启动恢复与手动选择 | `TaskBinding.ps1` 只恢复记住的编号，有限只读重试；选择/输入取消旧恢复；有效回执才提交，失败回退；不随机选第一项。 |
| 连接适配 | `codex_bridge.py` 在打开的同一管道句柄上验证包、程序名和 Windows 会话；唯一能力匹配才使用；歧义/未决超时关闭，实际写请求不换端点重试。当前 Store 包适配不等于通用公开 API 保证。 |
| 结束按钮 | 接话存在时显示“结束接话”，点击取消该接话；空闲后恢复“停止朗读”。这是同一按钮的状态复用，未表示重新接话。文案歧义列为后续体验项，本轮不改生产行为。 |
| 源码与部署 | 审核开始时工作树及桌面 main 均干净，桌面代码同为 `e4bb448`；本轮不改生产代码、不改设置、不重启当前实例。 |

审查是上述增量和安全约束的定向核查，不是对所有历史功能、所有设备或整个应用的无缺陷保证。

## 本次重新执行的离线检查

环境：Windows PowerShell 5.1，WPF 检查使用 STA；Python 使用本机已安装运行时，带 `-B`。PowerShell 启动参数为 `-NoLogo -NoProfile -NonInteractive -STA -ExecutionPolicy Bypass -File`。

| 入口 | 本次结果 |
| --- | --- |
| `tests/Test-HandsFree.ps1` | 482 断言，通过；真实 pending 状态转换与两轮接话、过期/取消/拒绝/未知保护。 |
| `tests/Test-TaskAutoConnect.ps1` | 25 场景、215 断言，通过。 |
| `tests/Test-BindingRecovery.ps1` | 15 场景、88 断言，通过。 |
| `tests/Test-SpokenTaskSwitch.ps1 -PythonPath <已安装解释器>` | 13 场景、380 断言，通过。 |
| `tests/test_bridge.py` | 62 测试，通过。 |
| `tests/test_task_matcher.py` | 50 测试，通过。 |
| `tools/Test-SourceLayout.ps1` | 46 源码依赖、11 运行时声明、7 声音资源及脚本解析/索引检查通过；不代表工作树自带运行时。 |

没有真实录音、音频播放、聊天发送或任务管理自动化测试；没有复制个人 runtime/data 或重跑受阻组合。各独立测试只精确清理自己的随机目录；结束后核对仍为原有 27 个文件，无本轮新增残留。上述通过不能替代未执行的旧组合或纯源码整套检查。

## 真人反馈与证据边界

- 连续两轮：对话确实收到两轮接话，用户确认已发送文字清空；后续用户总体反馈“测试没问题了”。不追加虚构的逐项朗读时延/识别率。
- 安静退出再唤醒：用户明确反馈再次说唤醒词自动激活，没问题。
- 主动结束：解释按钮恢复朗读用途后，用户总体认可测试结果；没有额外声称独立测过关闭开关或所有迟到回调。
- 长句：用户总体认可测试，但没有可单独确认的 10 秒首尾对照记录，不记作计时实测通过；相关时间边界已有离线覆盖，不要求重复整套验收。
- 背景手机视频：发生误收并发送，用户同意暂时接受；保留 N05/N03 风险，取消唤醒词不会自动解决。此事件不能证明是声伴自身朗读的回声，也不能证明自身回声抑制已通过或失败。
- 重开恢复、普通唤醒、任务切换、设置选择与微信截图：保留既有用户确认，不重复验收。

## 遗留与收口门槛

1. 历史测试缺口：临时复制源码、组合回归及递归清理曾被拒绝；相关旧入口没有本次全量运行结果。独立专项提供补充证据，不冒充原检查完成。
2. 历史生成文件：`work/desktop-shell-render` 23 个渲染/结果文件，`work/tests/desktop-controller` 4 个替身设置/记录/结果文件，共 27 个。只读核实存在、被忽略，`git ls-files work` 为空；未进入源码提交。既有拒绝边界保持。
3. 背景人声误收：已知且用户暂时接受；不是全免唤醒就绪条件。
4. 按钮文案、任务候选确认的短时免唤醒仍未实现；不混入本次修复或下一版设备设置。
5. 睡眠/设备矩阵、真实创建往返、干净电脑安装、发行许可及公开安装包仍独立待验，不以归档代表通过。

前两项若获用户明确同意作为例外，可记录“有条件开发收口”，保持未验证/未清理事实不变；否则本任务保留，不自动进入下一版。

## Git 与下一版

审核前 fetch 遇 GitHub 连接重置；本地与缓存 origin/main 相同不能证明此刻远端同步。审核提交 `8a6afb5` 已在本地生成，普通 `git push origin HEAD:main` 因 GitHub 443 无法连接失败，没有强推或改写历史。本审核及后续状态记录尚未上传，桌面安装目录仍为 `e4bb448`，未改运行程序。网络恢复后须正常同步再考虑接续；不能将本地提交说成远端已交付。

满足收口门槛后，按已记录计划接续 **0.6.18：N11 输入输出设备设置**：明确选择设备/系统默认、回声参考与输出一致、断开/保存失败保护及离线回归。并非完全免唤醒，不提前更改 VERSION。此前把用户要求理解成只建文件有误；已按澄清创建独立审核任务并收到 P2，原任务负责修复，独立任务负责复核回报。正式下一版接续仍须满足收口条件，确认接手后才归档本任务。
