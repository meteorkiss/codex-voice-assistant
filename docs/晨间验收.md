# 声伴晨间验收

## 2026-09-21 · 独立启动与选择失败

根因已复现：旧连接发现依赖的 Codex 进程参数已变化，任务索引可读但实际任务连接找不到；保存的目标没有丢失。已修新版连接发现、上次目标自动恢复和最多三次临时失败重试；永久失败不重试、不随机换任务，错误明确显示具体阶段。

本轮真人只核对：打开后当前连接恢复“声伴 v0.6.17”；需要时选一个活动任务能连上；喊“你好，声伴”恢复回应。无需重新归档真实任务或重验微信截图。离线通过桥接 62、连接 25 场景/215、归档恢复 88、语音链路 380、免手 340、源码声明 46；已用去除注入环境的只读检查验证端点发现，未真实发送消息或录音代测。部署结果另记于版本接续。

## 2026-09-13 · 纯本地匹配与候选确认

用户已授权并实现：名称基本一致、字面唯一时直接连接；有英文错字、中文同音或已知别名时先报真实候选，一个候选问是不是，多个候选用编号，不猜版本号。单候选请再次唤醒后说“是的”或“不是”，也可直接在设置里选中；本次不自动开启额外录音或安装模型。

本增量真人只需自然使用时核对：明确关键词直接切换；近似名称先确认，说不是保留原任务，说是后才校验连接。已通过的手动选任务、后续语音投递、微信截图不再重验。离线通过：口令 1543、匹配 50、桥接 53、生产语音链路 13 场景/380 断言、免手 340、归档恢复 88、手动自动连接 166、源码声明 46。实际加载结果见版本接续，真人效果不由离线结果代替。

已加载源码 `ce08be3`，19:29:53 安全重启至 PID 45328；原目标及设置文件不变，监听 ready/healthy、处理量增长、无相关错误。此前设置/截图验收结果保留，当前只待用户实际体验本地候选确认。

## 2026-09-13 · 截图中的语音切换回归

已通过（用户现场反馈）：设置选中即连接、下一句语音进入所选目标、微信截图。无需重验。

本次只核对“把任务切到 6.17”能否由声伴本地更新连接，而不是作为消息出现在聊天里。截图中的“申办0.6.17”仅做版本限定的已知产品别名匹配；“6.117”不猜成“6.17”，无匹配应提示实际听到的目标并保留原连接。修复后的运行加载与离线证据见版本接续；真人语音结果仍待本增量确认。

已加载：修复 `b784548` 上传 origin/main 并同步桌面 main，构建成功，19:07:05 安全重启至 PID 44692。原目标与设置文件不变，监听 ready/healthy、处理量持续增长且相关错误为空。口令 1515、匹配 43、桥接 53、截图链路 8 场景/189 断言、免手 340、归档恢复 88 通过；没有用真实录音或任务代测。

## 2026-09-13 · 设置交互增量

已移除“连接所选任务”按钮，选中后自动校验并连接；失败回显原连接和原因，旧草稿独立保存，不会转发到新任务。刷新/筛选不自动代选。设置面板不置顶，悬浮声波与字幕的置顶偏好保持。

现场结果：用户已确认选中即连接，随后语音进入所选目标，微信截图也正常。自动连接 19 场景/166 断言、控制器 21 场景、原生置顶 97、布局 119、Dispatcher 18 步、归档恢复 88、免手 340、基础设施 91 通过；没有真实任务/消息/背景录音自动化，现场验收来自用户反馈。

已加载：`ea4780d` 上传 origin/main 并同步桌面 main，启动器构建通过；2026-09-13 18:33:40 安全重启（PID 30108），原 0.6.17 连接与监听 ready/healthy 恢复，无相关错误。设置未重新打开，WPF Topmost=false；悬浮球仍置顶，连续接话仍关闭。设置显示后的原生非置顶已有专项验证，真人微信截图另行确认。

旧组合回归未重试；两份旧集成测试仅静态适配。Controller 的 4 个临时测试文件及 23 个渲染/结果文件清理受阻，已保留且不入库，未重试或绕过；不宣称 work 已空。以下为之前阶段记录，不要求重复验收。

## 2026-09-12 · 0.6.17 增量与故障回归

最新归档恢复：已修复“归档旧目标 + 隐藏草稿”使监听和连接同时被阻断。确认归档后普通发送关闭，但仍能唤醒说本地切换；草稿先保存到 `data/recovery-drafts`，新目标不自动带入。手动连接同样先保存草稿，选择下拉项尚未完成连接会明确提示。本次现场误识别文字已按用户明确授权清空，无需再保存或重启。

本轮只需先说“你好，声伴”，听到回应后说“切到声伴 6.17”，核对当前连接。之后自然使用中确认：明确点击连接不再被旧草稿困住；归档目标不会继续显示有效发送连接；旧文字能从“查看保留草稿”找回，不会自动发给新任务。不要求用户重新归档真实任务做重复整套验收。

离线证据：桥接 53、草稿保存 26、归档恢复 15 场景/88 断言、监听故障恢复 92、免手 340、控制器 21 场景、布局 118、基础设施 91、新建 Python 33、管理 Python 26 通过。均为生成数据和设备/桥接替身，不能替代真人声学；被拒的旧组合回归未重试。

实际加载与上传：修复提交 `386c855` 已上传现有 `origin/main`，桌面 main 同步并完成启动器构建；2026-09-12 19:15:36 重启为 PID 31148，已真正连接“声伴 v0.6.17 · 短时连续接话”，持久设置也已保存同一目标。19:16 复核草稿为空、绑定 active、监听及唤醒就绪、健康 healthy，采集/处理量持续增长且相关错误为空。连续接话仍关闭，个人偏好未改。这里只确认运行恢复，是否听懂真人仍需上面的一次唤醒体验。

### 此前阶段记录（待重载、旧草稿及上传失败状态已更新，不要求重复验收）

最新名称/关键词补丁：列表及语音共用侧栏名称；不另加语音大模型。桥接 37、匹配 35、口令 1354、控制器 21 场景、布局 118、状态机 340 项及源码布局检查通过，未操作真实任务或录音。只读现场已唯一找到“声伴 v0.6.17 · 短时连续接话”；另外两项候选无名称覆盖时会提示。

本次只验三项（需先加载新代码）：

1. 刷新目标列表，能看到与侧栏一致的“声伴 v0.6.17 · 短时连续接话”。
2. 保存/取消旧草稿并恢复监听后，说“切到声伴 6.17”，应本地更新声伴连接目标，不把口令当聊天发送；“声伴”有多个匹配时应让你选择，不能猜一个。
3. 查找失败时原目标不变；先核对当前连接再正常继续说话。Codex 页面仍可点“在 Codex 中打开”，本轮未实现页面/绑定自动同步。

现场仍有草稿，未强制重启或清空，语音/界面补丁未记作加载或真人恢复；已提醒先复制要保留的文字再取消草稿。下文是前一轮故障记录，不要求重复整套历史验收。

15:36 后新增现场问题：存在草稿时主动暂停唤醒，界面却没有解释；此前解析器也漏掉“切刀语音助手MVP”。自然标题解析和暂停提示已补源码，解析 1322、状态机 340 项通过；现场草稿保留，未重载这次补丁，未记作真人恢复。用户正在讨论独立本地切换对话方案，此项属于待确认交互设计，不是已上线能力。

源码 bf44136 已上传并同步桌面目录，0.6.17 已于 15:29 左右实际加载。15:30 复核旧路径错误为空、绑定健康、回声就绪、处理量持续增长；原绑定与个人偏好保留（设置结构 version 正常迁移），连续接话仍关闭。未代用户切换真实任务或发送消息。

这次只验以下增量，先确认实际运行版本为 0.6.17。下面 0.6.16 的内容保留历史含义，不要求重复整套验收。

1. 唤醒识别：说“你好，声伴”，听到回应后说一句短话；应进入识别，不再立即退回待机或反复报旧路径不存在。
2. 本地切换：说“帮我切换到声伴 V0.6.17 任务”。声伴应本地查找、显示连接目标，不能把口令当普通聊天发送。转写成“切换刀”也支持；多个同名候选应请你选择。核对目标后，下一句应只发到该任务。
3. 短时接话（默认关闭，仅想体验时开启）：一次唤醒问题的回答自然读完，出现“连续接话 · 正在听”后直接续问。6 秒内开口即可，不能第 6 秒截断正在说的话；无话退出，30 秒到限只留草稿。
4. 结束边界：点“结束接话”或切换目标后，旧识别/回答不能重开窗口、抹掉新草稿或自动发送。不开接话时不必测第 3、4 项。

故障原因已确认：0.6.16 读被归档任务的旧路径失败，异常反复停止输出/取消录音；监听 healthy 不等于语音流程成功。版本点被解析器误判为断句是本地切换失败的另一原因。上述源码已修复，部署结果单独记录。

本次通过：解析 1290、匹配 24、真实 Tick/文本事件替身 330、记录恢复 12、C# 接话保护 3 项。临时产物已清理，未使用真人/背景录音或真实任务管理。新增切换集成样例尚未执行：临时复制、三组测试与递归清理的组合命令被审批拒绝，未重试或绕过；本次不记为全量通过。

真人、背景电视/外放重叠、异机安装与发行仍未验收。

## 历史：0.6.16

更新：2026-09-11 07:54（北京时间）。当时本机版本 **0.6.16**，夜间检查已完成，待用户现场体验验收。

## 已完成

- B1—B4：统一任务绑定、保护草稿、提取基础服务、统一常用设置与连接边界。
- B5：提取 AudioOutput，集中管理合成启动、完成交接、停止和文件归属；保留 HandsFree 的采集协调。
- B6 与仓库准备：统一版本与构建检查；增加源码依赖/入库文件检查及 Windows 离线 CI 配置。
- 夜间交付整理：补齐[依赖与源码交付](依赖与源码交付.md)及机器可读依赖快照，说明本机完整目录与 Git 源码的区别，列明安装与分发缺口。

本轮修复合成启动失败残留、播放失败残留、停止失败时待播内容未取消、过期合成播放及重复启动覆盖。原设置和使用方式保留。

## 自测结果

| 对象 | 结果 | 范围 |
| --- | --- | --- |
| 隔离回归 | 16 组通过 | 不操作真实 Codex 或音频设备；临时副本已清理 |
| 最后音频修正 | 输出 44 项、播放取消 344 项、续读套件复验通过 | 含播放和停止同时失败、目录连接拒绝、取消和旧结果 |
| 真实音频组件 | 编译与播放位置参数检查通过 | 使用本机 NAudio；未打开设备 |
| 源码布局 | 44 项源码依赖、11 项运行依赖声明、7 个声音资源检查通过 | 源码依赖及音频资源存在；运行依赖另行安装，入库文件无运行数据 |
| 构建及重启 | 0.6.16.0 构建通过，20:42 重载成功 | 偏好保留，窗口及置顶正常，回声就绪，监听 healthy |
| 实时健康 | 处理量持续增长，错误及模块警告为空 | 正常运行观察，不能代替真人声学验收 |
| 21:47 续检 | 原进程持续运行，connected / echoReady 为真，监听 healthy，状态文件实时更新，无模块警告 | 本轮未重载、未修改配置或运行代码 |
| 夜间至晨间抽查 | 22:43、23:46、00:46、01:47、02:49、03:50、04:50、05:52、06:52、07:54 共 10 次均为进程存活、连接与回声就绪、监听 healthy，错误及两项模块警告为空，状态文件实时更新 | 定时只读抽查，不等于连续无故障证明或真人声学验收；期间未重启、未修改运行代码或配置，未生成测试文件 |
| 依赖与源码清单 | 12 项文件大小及 SHA-256、17 个 Python 包版本、pip check 通过；44 项源码依赖均已纳入 Git；20 个 Python 文件语法通过 | 来源沿用既有本机记录，未下载上游包；本次无临时测试产物 |

新增 CI 尚未在 GitHub 执行，仓库尚未推送。本机依赖清单已补齐；解释器原始归档来源、Python 安装包哈希、第三方分发条件、干净电脑安装和真人重叠说话仍待验证，不宣称已具备公开安装包条件。

纯 Git 源码导出回归未执行：自动审批拒绝了导出、回归与清理副本的组合命令，仅返回 blocked by policy，未提供具体原因；没有生成导出副本，未重试该操作。44 项依赖的 Git 索引核对已单独通过，但不能代替纯源码实跑。

## 现场体验

1. 正常唤醒，问一句简短问题，确认文字与朗读正常。
2. 分别在设置页和通过语音改变语速或置顶，确认状态一致。
3. 朗读中暂停、继续、停止，确认停止后不再串出旧语音。
4. 输入一段草稿，再进行任务选择或设置操作，确认草稿保护符合提示。
5. 按实际需要选择目标任务，确认后续问题投递准确。

真人唤醒成功率、外放重叠、设备插拔与第二台电脑安装由实际验收记录补齐。夜间不发送真实测试聊天、不执行任务管理写操作、不推送或发布。

## 夜间收尾

07:54 最后一次只读核对通过，夜间自动检查收尾后暂停，声伴程序继续运行供验收。没有新增故障证据，未追加代码修改或重复整套回归。Git 在本次记录更新前无未提交变更；未推送或发布。

待完成项仍为上文列出的真人与设备检查、第二台干净电脑、纯 Git 源码实跑、远端 CI，以及上游归档与分发条件核验。读取 `data/status.json` 使用有限短暂重试，避免原子替换瞬间被误判为程序退出；检查只输出必要健康汇总，未输出聊天或录音内容。
