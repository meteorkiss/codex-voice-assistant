# 版本接续

更新：2026-09-12。用户要求按版本拆分开发任务，标题带版本号，当前版本收口后自动创建下一版并归档前一版。此规则通过根目录 AGENTS.md 和下一任务的交接执行，不新增定时轮询。

## 当前交付

15:36 后用户再次报告“切刀语音助手MVP”后听不到。现场进程仍响应，未发送草稿导致主动暂停监听；本地命令计数为 0。纯解析确认此前不支持“切刀”及无“任务”后缀的普通标题，但尚未取得实际草稿，不能断言草稿产生原因。补丁已支持上述自然说法（已知设置优先），并明确显示草稿暂停/恢复操作；纯解析 1322、真实 Tick 替身 340 项通过。因现场草稿仍在，本补丁尚未部署或重启，不能标为恢复。用户正在讨论更好的“切换这个对话”方案，暂停继续叠加实现，下一步先确认独立本地口令通道、草稿保留及页面/绑定同步的范围。

2026-09-12 故障增量：报告时实机仍为 0.6.16，首版 eac4d56 曾因 GitHub 网络失败未上传/部署。修正提交 bf44136 已修复归档记录打断录音、版本口令本地匹配和接话边界，已安全推送 origin/main 并快进同步桌面 main。15:29 左右经版本一致性构建后加载 0.6.17：旧路径错误消失、原绑定及个人偏好保留（仅设置结构 version 迁移），接话仍关闭；15:30 再核对绑定健康、回声就绪和处理量持续增长，无启动/工作进程警告。这是运行健康，不是声学验收。

本次通过解析 1290、匹配 24、真实 Tick 替身 330、记录恢复 12、C# 3 项及源码布局检查；新增切换集成样例因组合测试命令被审批拒绝未执行，未重试或绕过，不记作通过。该验证缺口仍在，暂不接续 0.6.18 或归档本任务。用户只验[晨间验收](晨间验收.md)顶部增量，不重验历史整套功能。

0.6.17 已完成 N02 短时连续接话首版。功能默认关闭，仅在明确唤醒问题成功发送、对应回答自然朗读结束后开放 6 秒接话窗口；单句最长 30 秒。全双工就绪时直接交接现有回声消除采集以保留句首，其它路径等麦克风真正开始后才显示正在听。

2026-09-12 首次推送完成，源码基线提交 5019e50，远端 main 与本地一致。发布前将朗读测试中的个人目录改为通用示例，70 项相关断言及源码布局检查通过。旧本地开发历史保留在本地 codex/local-history-v0.6.16 分支，没有推送；公开 main 从清理后的源码树开始。后续不重写公开历史。

本版将草稿编辑、目标或筛选变化、发送/连接异常、外部麦克风占用、闲置和显式取消设为窗口终止条件；旧转写与回答回调不能重开窗口。模糊短语只留草稿，不自动派发。17 组离线回归通过并清理临时副本；未进行真人接话、背景媒体、异机安装或发行验收，不将其误记为已通过。

仍缺少的真人、设备、异机安装、发行许可和远端检查分项记录，不因开发任务归档就标为通过。旧纯 Git 导出回归命令曾被自动审批阻止，不能重试或绕过；普通 CI 或源码布局检查的结果如实单列。

## 下一版：0.6.18 输入输出设备设置

范围对应功能总表 N11，先实现一个可验收的小范围：列出可用输入与输出设备，允许用户明确选择或恢复系统默认，并把回声参考端点与实际朗读输出保持一致。设备不存在、断开或保存失败时回退到原选择，不静默改用另一个同名设备。

不在本版承诺睡眠、蓝牙切换和所有驱动的完整矩阵；先复用现有 MicGuard、EchoCapture 与 AudioPlayer 的端点编号，补设置事务、重建时序、断开回退和替身回归。真人设备切换与离线回归分开记录。

0.6.17 的真人体验只需单独核对接话窗口时机、句首、取消和背景媒体边界，不要求重验此前已通过的全部基础功能。

## 后续选题与收口

0.6.17 完成后，从功能总表中尚未实现且前置条件具备的需求选下一项，推荐先处理明确设备设置、模型预设或指定项目新建；完全免唤醒及三档灵敏度要先具备可校准的判定器，不能用简单关键词冒充。仅支持已记录范围，微信保持暂不开发。

每版交接写清：实际提交与版本、本版变化、已通过的相关检查、临时产物清理、尚未验证事项、下一版范围和是否需要真人参与。Git 同步和新任务接手确认后再归档前一任务；失败时保留能继续处理的任务并说明实际阻碍。
