# 版本接续

更新：2026-09-12。用户要求按版本拆分开发任务，标题带版本号，当前版本收口后自动创建下一版并归档前一版。此规则通过根目录 AGENTS.md 和下一任务的交接执行，不新增定时轮询。

## 当前交付

2026-09-13 语音切换截图回归：用户已经确认设置选中即连接、随后语音进入所选目标、微信截图正常，这三项无需重验。截图里的“把任务切到这个申办0.6.17。”和“把任务切到6.117。”被当普通消息发送，现场本地命令计数为 0；定位为缺少对象前置的切换语法，不是该截图能证明的麦克风或重连故障。

本轮仅补上述本地入口和带明确版本的已知产品别名。字面标题优先，其次才尝试“申办→声伴”，其它关键词仍全部参与；原查询、标题及版本数字不改。无匹配播报实际识别目标并保持原连接，不能把 6.117 猜成 6.17 或交给聊天代做。否定/问句/复合操作和草稿、旧代次、未知回执约束保留；未增加语音大模型、免唤醒或页面自动同步。

本增量独立通过：口令 1515、标题匹配 43、桥接 53、截图语音链路 8 场景/189 断言、免手 340、归档恢复 15 场景/88 断言及源码声明 46 项。新专项使用真实生产醒词/提问/Tick/转写分派/绑定函数及纯 Python 匹配器，候选、录音、播放和桥接均为合成替身；验证唯一后读取、歧义不选、数字不猜、下一句投递新编号、新输入使旧查询/读取失效及失败不转聊天。独立检查的新生成物已自行清理，未录音或操作真实聊天。

实际部署：修复提交 `b784548` 已正常上传现有 origin/main，桌面 main 安全快进并构建启动器 0.6.17。两次 fetch 遇网络失败，随后普通 push 成功，未强推或重写历史。2026-09-13 19:07:05 确认草稿为空、录音/播放/桥接空闲、发送账本无待处理项后重启为 PID 44692；19:07:12 复核原目标及设置文件 SHA256 不变，绑定 active、监听 ready/healthy，连续接话仍关闭。随后采集量从 2,560 增至 230,720、处理量从 160 增至 224,160，相关错误为空；旧主进程及唤醒子进程均已退出。只恢复用户原已启用的监听并读取健康指标，没有真人录音/聊天自动化，截图口令的真人验收仍待确认。

旧组合检查和 27 个历史生成文件清理受阻边界不变，未重试或绕过，不接续/归档本任务。复核 work 仅保留此前 23 个渲染/结果文件及 4 个 Controller 生成文件，本次新增专项与构建生成物已清理。下文为设置阶段交付记录，现场验收状态以上文为准。

2026-09-13 设置交互增量：用户要求取消“连接所选任务”，选中即连接，并取消设置面板置顶。现已实现明确选择后自动只读校验/提交绑定，失败回显原实际连接，不自动排队重试；刷新、筛选和程序性回显不触发连接。快速改选、新输入和超时撤销旧回执；语音多候选接管后继续显示真实待连目标；不健康的持久目标不预选，允许用户再次选同一任务。草稿先保存、未知发送不重派等保护不变。设置独立非置顶，不继承悬浮球 Owner，置顶偏好只影响悬浮声波与字幕；未操作微信进行截图实测。

独立验证通过：自动连接 19 场景/166 断言、控制器 21 场景、原生置顶 97、布局 119、窗口 Dispatcher 18 步、归档恢复 15 场景/88 断言、免手 340、基础设施 91、源码声明 46 项。两份旧切换/创建集成测试仅适配已删除按钮并静态解析，未运行，原组合回归受阻边界保留。Controller 的 4 个临时文件及本轮 23 个渲染/结果文件清理分别被策略拒绝，未重试或绕过；它们在忽略的 work/tests/desktop-controller 和 work/desktop-shell-render 中，不进入源码。本任务暂不接续或归档。

实际部署：源码 `ea4780d` 已安全上传现有 origin/main，并快进桌面 main，启动器 0.6.17 构建通过。2026-09-13 18:33:40 检查无草稿、无录音/播放、无待处理发送后重启为 PID 30108；18:33:54 复核原 0.6.17 目标恢复、bindingAvailability=active、监听 ready/healthy、错误为空、连续接话仍关闭。设置 WPF Topmost=false，悬浮球 WPF/native Topmost=true；设置尚未重新打开，因此当前运行实例 settingsHasHandle=false，不能把 native=false 当作已显示窗口的实测；已显示设置的原生非置顶证据来自 97 断言专项，微信截图仍待用户现场核对。测试产物未随源码上传。

### 2026-09-12 归档恢复交付记录

最新归档 BUG 增量（用户已授权实现，并确认现场误识别可清空后重启）：归档状态不再由记录文件是否可读推断。新增独立只读 `binding-state` 精确核验当前编号，5 秒轮询、8 秒进程上限，与互动桥接分开；确认归档/缺失后关闭普通投递及旧异步代次，保留真实编号供本地切换/创建上下文，不自动恢复归档或改连默认任务。未知状态只暂停普通投递、不冒充归档。发送前及最终派发边界复核状态，旧 pending/unknown 账本优先，仍不重派。

草稿恢复：确认归档或用户明确点击连接时，先保存独立 UTF-8 文本及原任务归属，成功才清输入；失败原文不动。恢复期间普通 ASR 只保存，本地切换口令独立消费；新目标不自动回填/发送旧草稿。设置页增加保留草稿入口和选中/连接区别。归档本地口令模式同步接入麦克风故障恢复。最新独立验证：桥接 53、草稿 26、归档恢复 15 场景/88 断言、WakeRecovery 16 场景/92 断言、免手 340、控制器 21 场景、布局 118、基础设施 91、新建 Python 33、管理 Python 26 通过；源码声明 46 项。旧 WakeRecovery 测试漏加载已提取的 AudioOutput，补齐真实模块后通过；未更改其设备替身边界。没有真实任务管理、消息或录音自动化。

本次部署完成：修复提交 `386c855` 已上传现有 `origin/main`，桌面安装目录 main 安全快进到同一提交，启动器完成 0.6.17.0 构建。2026-09-12 19:15:36 按用户授权清空误识别草稿并重启（PID 31148），显式校验并连接“声伴 v0.6.17 · 短时连续接话”；实际绑定及持久设置均为 `01a09455-6bd9-76f3-ad04-a3f053a16542`，不再沿用已归档旧目标。19:16 复核 `bindingAvailability=active`、`draftPending=false`、`wakeListening/wakeReady=true`、`wakeHealth=healthy`；采集量从 58,720 增至 417,760、处理量从 56,160 增至 416,160，启动/绑定/唤醒/工作进程错误为空，连续接话仍默认关闭。此次仅恢复用户原已启用的监听并读取运行指标，未以真人聊天、任务管理或背景录音做自动化测试；运行健康不等于真人声学验收。

本轮测试临时文件已清理，工作树 `work` 下无剩余文件。原组合全量回归曾被拒，未重试或绕过，不能把新独立专项通过说成旧整套通过；暂不接续/归档开发任务。

### 此前阶段记录（下述待重载、草稿与网络状态已由以上部署结果更新）

部署记录：源码提交 `8bb74a9` 已安全快进到桌面安装目录 main，两处工作树检查干净；从安装目录加载新 Python 桥接只读查找“6.17”，唯一编号及完整侧栏标题均正确。下一次列表刷新会使用该桥接；当前运行的 PowerShell 未重启，仍有草稿且主动暂停监听，口令/提示/XAML 不应视为已加载。25 个本轮生成 UI 图、替身设置及结果文件已清理，work 下无剩余文件。GitHub fetch 连接重置、随后普通 push 连接 443 失败；未强推，远端尚未核对到本轮提交，后续网络恢复再安全上传。

最新：用户已确认先调整对话名称与关键词查找，不增加语音大模型。SQLite 仍决定原有可信候选；只读 `session_index.jsonl` 按最新合法 UTC 时间及同时间后写记录覆盖名称，坏行、空名、未完成末行不覆盖，缺失覆盖会提示，索引不可读时明确失败。列表与语音共用名称；支持“高斯泼溅”“6.17”“声伴 6.17”“6.17 声伴”“声伴6.17”，保留完整标题/连续片段优先，关键词交集及完整版本边界，多项匹配仍选择。补通裸数字点口令总入口；设置优先，否定、问句、引用和复合操作保护不变。

本轮独立通过：桥接 37、匹配 35、口令 1354、控制器 21 场景、WPF 布局 118、免手状态机 340 项及源码布局检查。布局初跑发现旧“声音页无需滚动”断言已不适合接话新增行；改为验证无横向溢出及末项可完整滚动到达，查看渲染后通过，未改声音页功能。只读现场确认 17 个候选中 15 个有名称覆盖，当前任务名称准确，三个 6.17 关键词组合均唯一命中预期编号。现场仍有草稿，未强制重启或清空；语音/界面补丁未记作加载或真人恢复。已提醒先保存并取消草稿。旧集成验证缺口仍保留，不接续或归档本任务。

此前名称/关键词阶段不包含跨草稿独立本地口令、自动跟随前台对话或切换后自动打开 Codex 页面。后续用户已授权并实现上述归档恢复及明确连接时保存草稿，但仍无通用跨草稿独立口令或页面自动同步；声伴发送目标与 Codex 当前页面仍是两个状态。下段“暂停继续叠加实现”是此前讨论阶段的历史状态。

15:36 后用户再次报告“切刀语音助手MVP”后听不到。现场进程仍响应，未发送草稿导致主动暂停监听；本地命令计数为 0。纯解析确认此前不支持“切刀”及无“任务”后缀的普通标题，但尚未取得实际草稿，不能断言草稿产生原因。补丁已支持上述自然说法（已知设置优先），并明确显示草稿暂停/恢复操作；纯解析 1322、真实 Tick 替身 340 项通过。因现场草稿仍在，本补丁尚未部署或重启，不能标为恢复。用户正在讨论更好的“切换这个对话”方案，暂停继续叠加实现，下一步先确认独立本地口令通道、草稿保留及页面/绑定同步的范围。

2026-09-12 故障增量：报告时实机仍为 0.6.16，首版 eac4d56 曾因 GitHub 网络失败未上传/部署。修正提交 bf44136 已修复归档记录打断录音、版本口令本地匹配和接话边界，已安全推送 origin/main 并快进同步桌面 main。15:29 左右经版本一致性构建后加载 0.6.17：旧路径错误消失、原绑定及个人偏好保留（仅设置结构 version 迁移），接话仍关闭；15:30 再核对绑定健康、回声就绪和处理量持续增长，无启动/工作进程警告。这是运行健康，不是声学验收。

本次通过解析 1290、匹配 24、真实 Tick 替身 330、记录恢复 12、C# 3 项及源码布局检查；新增切换集成样例因组合测试命令被审批拒绝未执行，未重试或绕过，不记作通过。该验证缺口仍在，暂不接续 0.6.18 或归档本任务。用户只验[晨间验收](晨间验收.md)顶部增量，不重验历史整套功能。

0.6.17 已完成 N02 短时连续接话首版。功能默认关闭，仅在明确唤醒问题成功发送、对应回答自然朗读结束后开放 6 秒接话窗口；单句最长 30 秒。全双工就绪时直接交接现有回声消除采集以保留句首，其它路径等麦克风真正开始后才显示正在听。

2026-09-12 首次推送完成，源码基线提交 5019e50，远端 main 与本地一致。发布前将朗读测试中的个人目录改为通用示例，70 项相关断言及源码布局检查通过。旧本地开发历史保留在本地 codex/local-history-v0.6.16 分支，没有推送；公开 main 从清理后的源码树开始。后续不重写公开历史。

本版将草稿编辑、目标或筛选变化、发送/连接异常、外部麦克风占用、闲置和显式取消设为窗口终止条件；旧转写与回答回调不能重开窗口。模糊短语只留草稿，不自动派发。17 组离线回归通过并清理临时副本；未进行真人接话、背景媒体、异机安装或发行验收，不将其误记为已通过。

仍缺少的真人、设备、异机安装、发行许可和远端检查分项记录，不因开发任务归档就标为通过。旧纯 Git 导出回归命令曾被自动审批阻止，不能重试或绕过；普通 CI 或源码布局检查的结果如实单列。

## 下一版：0.6.18 输入输出设备设置

范围对应功能总表 N11，先实现一个可验收的小范围：列出可用输入与输出设备，允许用户明确选择或恢复系统默认，并把回声参考端点与实际朗读输出保持一致。设备不存在、断开或保存失败时回退到原选择，不静默改用另一个同名设备。

不在本版承诺睡眠、蓝牙切换和所有驱动的完整矩阵；先复用现有 MicGuard、EchoCapture 与 AudioPlayer 的端点编号，补设置事务、重建时序、断开回退和替身回归。真人设备切换与离线回归分开记录。

0.6.17 的真人体验只需单独核对接话窗口时机、句首、取消和背景媒体边界，不要求重验此前已通过的全部基础功能。

## 后续选题与收口

0.6.17 完成后，从功能总表中尚未实现且前置条件具备的需求选下一项，推荐先处理明确设备设置、模型预设或指定项目新建；完全免唤醒及三档灵敏度要先具备可校准的判定器，不能用简单关键词冒充。仅支持已记录范围，微信保持暂不开发。

每版交接写清：实际提交与版本、本版变化、已通过的相关检查、临时产物清理、尚未验证事项、下一版范围和是否需要真人参与。Git 同步和新任务接手确认后再归档前一任务；失败时保留能继续处理的任务并说明实际阻碍。
