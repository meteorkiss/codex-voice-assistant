# 0.6.12 本机更新与验证

2026-09-09，本机运行版本已更新到 0.6.12。原任务绑定和全部偏好保留；回声采集就绪，唤醒状态 healthy，模型处理量继续增长，无启动错误。加载凭据为 `work/tests/desktop-actions-20260909/fix-0612-live.json`。

本版增加分组和对话管理口令，以及语音暂停、继续、重读。完整命令见 [本地管理口令](本地管理口令-0.6.12.md)。当前开发对话已通过 Codex 桌面的项目菜单移入“语音助手”项目；声伴本身尚未接入项目移动接口，遇到此类口令会在本地说明操作入口。

## 已完成验证

| 范围 | 结果 |
| --- | --- |
| 管理口令解析 | 650 项通过，含名称保真、否定、引述、含糊对象和复合句 |
| 原有口令和本地发送路由 | 1261 项解析、762 项路由通过 |
| 管理命令生产接线 | 101 项通过；手动与自动入口、一次派发、迟到回执、草稿和绑定保留、失败播报 |
| Python 管理接口与原桥接 | 26 项管理、30 项桥接、33 项原创建流程通过 |
| 原创建流程接线 | 109 项通过 |
| 唤醒后续读 | 正常播放器与旧播放器替身各 49 项；真实生产函数执行，设备和接口使用替身 |
| 朗读取消与免手状态机 | 344 项、184 项通过 |
| 音频组件 | 真实 NAudio 编译通过；非法续播位置在打开设备前被拒绝 |
| 脚本与启动器 | 改动 PS1 语法和 UTF-8 BOM 检查通过，Windows 启动器构建成功 |

续读覆盖：唤醒保存→应答→录音时再次保存→继续；保留中断位置和剩余语音队列。普通停止、发送新问题、切换任务、新回答和新的用户轮次会清除旧书签。单元和集成检查未使用真实麦克风、未播放测试讲话，也未发送 Codex 测试消息；本机正常声伴重载后恢复了真实监听。

## 真实接口检查

只读检查完成项目和分组列表、任务列表、当前回答及状态读取，并验证项目内任务可按完整名称找到。

临时创建 `声伴接口验证-db550004` 分组，改名后删除；三步均读取核对相同分组 ID，已清理。没有通过此烟雾检查修改真实任务或发送消息。凭据在 `work/tests/desktop-actions-live-*` 本次目录。

管理操作使用独立请求编号和持久账本；未知结果不会自动重派，不会降级为聊天消息。按完整名称匹配，重名不猜测；归档和恢复只允许其它空闲或未加载的任务。

## 验证边界

真人边说边唤醒的声学成功率，以及不同设备上的无缝续播，未作为本次新增验收执行。当前通过的是生产逻辑、实际组件编译、接口烟雾检查与运行健康确认。

曾误调用需要单独 TestMode 助手进程的历史 `Test-HandsFreeIntegration.ps1`，因本次没有启动对应测试进程，读取旧 v5 状态后超时；不计入通过结果，也没有操作正式声伴。失败尝试保存在 `work/tests/desktop-actions-20260909/legacy-harness-attempt/failed-attempt.json`，残留命令已清理。随后本次正确的生产状态机测试 `Test-HandsFree.ps1` 通过 184 项。历史默认目录的 result.json 已被该失败尝试覆盖，不再作为历史成功证据引用。