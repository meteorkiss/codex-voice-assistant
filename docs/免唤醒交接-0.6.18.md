# 声伴 0.6.18 · 免唤醒交接

2026-09-21 用户最新要求：0.6.17 修复审核后完成 Git，同步后新建下一版任务，优先实现免唤醒；新任务先列完整架构和各结构职责，再写代码、做测试，准备 2026-09-22 的简短真人验收。此前推荐的 N11 设备设置顺延，不与本版混做。此文件是交接约束，不冒充架构已完成或功能已上线。

## 接手基线

- 实际 VERSION 仍为 0.6.17。运行代码修复 `53e32d9`，部署记录 `436d349`；本次普通 push 已确认 `e4bb448..436d349` 到 origin/main，此前网络失败记录已被本次成功更新。
- 0.6.17 独立审核任务 `01a0c405-c5f2-70c0-b758-aaabd8146902` 找到回答先于回执的 P2，原任务修复，对方复核通过。免手 837、归档恢复 88、语音切换 380、自动连接 215、源码声明 46 通过，原修复前测试缺口不能冒充整套通过。
- 桌面应用已安全加载 53e32d9，原目标/偏好保留、监听健康；运行 PID 是历史快照，不可直接作为以后停止进程的授权或身份依据。
- 用户认可已测连续两轮/清稿及安静退出再唤醒；手机视频曾误收并发送，用户暂时接受，但下一版不能假设此问题已解决。取消唤醒词本身不能解决背景人声。

## 先设计，再实现

新任务首先编写 `docs/免唤醒架构-0.6.18.md`：写清用户流程、目录/模块职责、接口、数据流、状态转换、线程/进程和麦克风所有权、超时/取消、故障恢复、配置迁移和回退。先读现有代码，复用安全服务，避免把新逻辑继续堆进 Assistant.ps1 或 HandsFree.ps1 的大分支；无需为了形式重构无关模块。

建议按职责审视以下边界，具体实现由新任务核验后决定：

1. 采集与分句：复用现有回声消除/音频所有权，单一采集入口，句首保留、停顿结束、最长时限、外部占用恢复。
2. 是否面向助手的判定：与 ASR/VAD 分开，输出结构化判定、原因和不确定状态；不能用“有声音/有人说话/几个关键词”冒充可靠判定器。
3. 免唤醒会话控制：开关、试判、可交互模式、暂停/结束、确认窗口及明确状态显示；先能不派发地观察判定，再按证据开放用户明确启用的交互模式。免唤醒必须真正不要求每轮先说固定唤醒词，不用无限延长 6 秒接话或每句换一个固定口令冒充。
4. 路由与执行：普通话保留原文，明确本地操作走既有执行入口，歧义/低置信不猜；候选确认可在有明确上下文的短时窗口直接说是/否/取消/编号，不跨任务误确认。
5. 发送与回答：沿用真实目标、请求账本和代次；未知不重派、草稿不丢、旧回调失效、早到回答与朗读结束关联；不得新建另一套绕开既有保护的发送通道。
6. 设置/诊断/测试：简单可理解的 UI、默认关闭、独立状态机及纯逻辑测试；隐私最小化，不默认保存整段背景录音或识别文本日志。

本次授权新任务完成设计后继续实施、隔离测试和必要安全部署，不需要只交方案等待一次重复“开始”。但可靠自动判定若缺模型、许可证、密钥、硬件或新增费用等必要条件，应如实报告具体缺口；不要伪造能力或自行购买服务。不得擅自上传新的背景音频到第三方。以可演示、可退回原唤醒模式的增量为目标，不保证一夜达到所有环境的声学可靠性。

## 测试与交付

- 自动化仅用合成音频/转写/设备和桥接替身，不用真实聊天、任务管理或背景录音当测试。覆盖安静、背景视频、自身朗读、多人/非面向助手、不确定识别、切换目标、取消、未知/迟到回执、设备不可用及退出。
- 真人采集只在用户明确开启模式后发生；新模式默认关闭，不能为了明早“已开启”擅自后台录音。上线时清楚说明开关和风险，准备最多 5 条验收步骤，未实测项标明。
- 保留 Windows PowerShell 5.1 兼容、中文脚本 UTF-8 BOM。工作树缺 runtime 是正常设计，不复制个人 data/模型/整套运行环境。
- 完成源码/相关隔离测试/自有生成物清理/文档/Git 后，安全核对草稿、发送、录音、朗读和当前实例再部署。用户正在使用时不强杀，不自行清稿。需要独立复核时沿用已有审核任务或按明确授权协调，不将自审冒充独立审核。
- VERSION 仅在本版实现完成时更新，不能因建立任务就提前改成 0.6.18；不自动继续创建更多未要求版本。

## 历史受阻事项的交接方式

原工作树有 23 个 UI 渲染/结果文件（`work/desktop-shell-render`）和 4 个替身设置/记录/结果文件（`work/tests/desktop-controller`），共 27 个，均被忽略且未纳入 Git；不是个人聊天或模型。清理曾明确被拒绝，旧源码复制/组合回归/递归清理及 Git 导出回归也曾被拒绝，继续不重试、不用新工作树绕过。

最新用户明确要求上传后建立免唤醒开发任务，因此可以按这次直接请求接手新范围；这不表示上述文件已删除、旧组合已通过或用户已明确豁免旧收口门槛。原 0.6.17 任务暂保留、不归档，保留完整证据。新任务的新产物只在自己唯一目录内精确自清理，不能碰这 27 个文件。
