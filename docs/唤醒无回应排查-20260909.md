# 2026-09-09 唤醒无回应排查

用户反馈说“你好，声伴”时声波有起伏，但没有回应。检查时声伴 0.6.7 自前一天 19:00 持续运行；状态文件实时更新，任务已连接，监听及回声采集标记就绪，唤醒计数为零，关键词工作进程仍在运行。没有发现创建或发送请求阻塞。

## 已确认的问题

重新启动声伴后，Windows 仍报告一个属于旧进程 36728 的活动录音会话。旧进程已退出，新进程为 46688；使用独立的 MicGuard 实例只读枚举，也得到旧进程的活动记录，进程名称为 Unknown。这使新进程把残留会话当作其他应用正在录音，暂停唤醒。重启检查因此在 27 秒内未等到监听就绪；残留记录稍后由系统自行清理，监听才恢复。

这是此次重启期间确证的阻塞原因，不能据此断定它也是重启前声波会动却不响应的唯一原因。

修复范围：活动会话的正进程编号须重新核验；只有能明确确认进程已不存在或已退出时才忽略。进程编号为零、访问被拒绝或查询失败仍按未知活动会话处理。检查不能依赖永久缓存的进程名称，避免进程退出和编号重用后误判。不重启 Windows 音频服务，不强制关闭其他应用的麦克风。

界面会明确显示其他应用占用麦克风导致的唤醒暂停。运行状态增加麦克风扫描计数、外部录音进程编号、唤醒事件版本、原始音量与错误，便于后续区分未监听与未识别；不记录音频内容。

## 长流对照

使用现有本地唤醒模型和同一合成正例，阈值保持 0.35。基线、五分钟静音、一小时静音后暂停送入三秒、五分钟低幅噪声四种前缀后都能检出唤醒词，前缀中无误触。结果在 `work/tests/wake/long-stream-20260909.json`。

这是加速送入文件样本的模型流测试，不能代表整夜实际运行、电脑睡眠恢复、用户发音或真实回声处理效果。没有证据支持把门槛继续调低作为此次修复。

首次检查的状态和设置快照保存在 `work/tests/wake-unresponsive-20260909/`。真人是否恢复回应，以重新启动后的实际试用反馈为准；目前未记录到用户确认。

## 已加载的修复

0.6.8 于 2026-09-09 09:25:36 本地时间重新加载，进程为 13712。任务、声音和所有偏好保留；麦克风监听与回声处理就绪，外部录音进程列表为空，运行状态无错误。证据在 `work/tests/wake-unresponsive-20260909/fix-068-live.json`。

进程所属核验的 18 项隔离检查通过，覆盖存活、退出、进程编号重用和无法读取信息的情况；既有免手流程 184 项、朗读取消 331 项、桌面控制 18 场景通过。长流模型测试与这些隔离回归不替代用户实际唤醒的确认。
