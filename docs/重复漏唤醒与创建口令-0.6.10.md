# 重复漏唤醒与创建口令：0.6.10

2026-09-09。用户再次反馈喊“你好，声伴”多次无反应但音环在动，并询问“创建新对话”等不同说法能否直接操作。

## 本次观察

原进程 48124 运行 0.6.9，绑定、麦克风和播放路由正常，无外部录音、发送或任务创建占用。连续三次观察中，采集量从 9581440 增长至 9609920，处理量从 9576320 增长至 9608320，健康状态 healthy。唤醒次数和激活版本均停在 4，HasActivated=false。

这说明观察期间没有有效的唤醒命中，不能将音环变化或健康状态等同于唤醒识别成功，也不能据此断定进程卡死。未发现已命中而界面漏掉回应的证据。状态新增草稿是否存在、停止状态、界面已处理的激活版本，便于后续定位。

默认关键词拼音和词表有效。补测约一分钟连续合成普通人声或近似口令后，既有正例仍被检出；没有复现长流导致模型停止识别。该文件试验不能评价真人发音经过实际回声处理后的清晰程度。本次没有录制用户声音、播放测试音频或继续降低唤醒门槛。

证据：`work/tests/wake-repeat-20260909/observations-before.json`、`speech-prefix.json`。

## 已修复的创建口令覆盖

`VoiceCommands.ps1` 现在按完整句子匹配“新建／创建／新开”与“任务／对话／聊天／聊天内容”。支持“一个／个／新／一个新”、原有礼貌前缀、命名及当前项目范围。

- 帮我创建一个新任务。
- 创建新对话。
- 新开一个对话。
- 新建一个聊天。
- 在当前项目创建一个叫天气的新对话。

匹配发生在副本上，原始转写及任务名称不替换。不从否定、讨论、测试说明或多个动作中抽出片段执行。未命中的普通内容仍按聊天处理；本次用户询问口令的整段话不作为创建授权。

Windows PowerShell 5.1 隔离回归：解析器 757、LocalCommands 434、TaskCreate 115、TaskCreateIntegration 109、生产计时器与朗读取消 344 项通过。未在测试中实际创建任务或发送消息。

## 加载与下一次真人检查

18:15（北京时间）空闲时加载 0.6.10，进程 53972。任务与全部偏好保留，悬浮窗可见且置顶。两次状态读取间模型处理量由 8160 增长到 40160，恢复阶段 idle，状态 healthy，无错误。证据为 `work/tests/wake-repeat-20260909/fix-0610-live.json`。

重新加载清理了旧监听会话，可用于恢复尝试，但不等于真人漏唤醒已修复。仍需用户在重新加载后喊一次唤醒词，核对是否有“在”的回应；若重复失败，需要针对失败时刻的实际输入进一步判断采集品质与关键词检出。尚未得到此次失败发生在安静待机还是朗读期间的回答。
