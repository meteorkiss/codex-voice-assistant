# 口语新建关键词：0.6.11

2026-09-09。补齐“帮我弄一个新对话”和独立短口令“新对话／新任务”的本地触发。

## 行为

- “新任务／新对话／新聊天／新聊天内容”可单独触发创建。
- 支持“弄／建／开／搞”搭配“新”对象，例如“给我开个新任务”“帮我弄一个新对话”。
- 支持“你帮我／我想／我想要／我要／直接”等请求前缀和“吧／啊／呀／哦／哈”等句末语气词。
- 原有“新建／创建／新开”、任务命名、当前项目范围继续可用。
- 仍匹配完整句子；讨论、否定、引用、多个动作及没有对象的“创建”不触发。原始转写和名称保持原文。

## 路由核对

手动 Send-Text 与自动 Try-AutoDispatch 均先调用本地口令解析。命中 createTask 后通过 TaskCreate → bridge action=create → create_thread 接口直接创建；创建错误不会回退为普通聊天。新任务中的“准备好了，请说”是初始化提示。拿到真实任务编号并读取校验、保存绑定后，后续问题才进入新任务。

## 验证与加载

Windows PowerShell 5.1 隔离验证：解析器 1261 检查、LocalCommands 35 场景 / 762 断言、TaskCreate 115 检查、TaskCreateIntegration 109 检查，全部通过。新增路由回归覆盖 20 种口令的手动及自动触发与 12 种非口令输入；未为测试创建真实任务或发送真实消息。真人语音识别准确率仍需实际使用验证。

18:26（北京时间）已加载 0.6.11，进程 35836。当前绑定与全部偏好一致，连接正常，监听 healthy，恢复阶段 idle，无错误；两次观测间模型处理量由 160 增长至 40160。记录：work/tests/colloquial-create-20260909/fix-0611-live.json。原代码与启动器备份位于 work/backups/before-colloquial-create-20260909，路由测试另有独立备份。
